# 隐私说明

## 原则

Codex 自动续跑守护是一个独立、非官方的本机工具。它没有账号系统、遥测、分析 SDK 或自有云端服务，不会读取或上传 Codex 的 `auth.json`、SQLite、rollout/JSONL、隐藏推理或完整对话。

应用只处理“在同一个 Codex 任务中安全恢复”所必需的数据，并把可持久化数据保存在当前 macOS 用户的应用数据目录中。

## 短暂读取但不落盘的数据

为了确认以下事实，应用会通过 Codex App Server 在进程内存中短暂读取线程及 turns：

- 最近一次顶层 turn 是否以结构化 `UsageLimitExceeded` 失败；
- 用户是否已经手动继续过该任务；
- 自动续跑 turn 是否结束，以及是否返回规定的结构化结果。

完整 turn 对象、用户提示词、工具调用内容和隐藏推理不会写入本应用的状态文件。进程结束后，这些内存数据不会由本应用保留。Codex App Server 及模型服务自身的数据处理由相应服务条款与隐私政策约束，不属于本项目控制范围。

## 保存在本机的数据

主要状态位于：

```text
~/Library/Application Support/codex-resume-manager/
```

根据任务状态，该目录可能保存：

- Codex thread/turn 标识；
- 项目目录、Codex 版本；
- `/goal` 的状态，但不保存目标正文；
- 额度窗口、刷新时间与采集时间；
- 自动续跑的幂等键、失败 turn 和恢复检查点；
- 泛化的人工处理原因代码；
- 最多 200 条仅含固定文案的状态变化记录；
- 开启、关闭和状态查询所需的临时命令与匹配确认；
- 无法处理的命令隔离文件和应用安装位置。

任务标题、目标正文、结果正文、验证文本和自由格式错误不会写入状态文件。项目目录和任务标识仍可能暴露工作关系，应按私人工作数据对待。状态文件和命令文件使用仅当前用户可读写的权限；应用不会主动把它们发送给本项目维护者或其他第三方。

## Hook 接收的数据

Codex 的 `UserPromptSubmit` Hook 会收到 Codex 提供的当前控制语句及任务元数据。只有明确的“开启自动续跑”“关闭自动续跑”“查看自动续跑状态”及支持的等价命令才会被处理。

控制语句只用于本机识别动作；落盘命令不保存完整用户提示词。未识别为控制命令的普通消息不会由 Hook 写入本应用目录。

## 系统通知

应用可能使用 macOS 通知提示守护状态变化，但只显示“等待额度、正在续跑、需要处理、已经完成”等泛化状态，不显示任务标题、项目路径、结果、验证文本或自由格式错误。通知是否出现在锁定屏幕、通知中心或其他已登录设备上，由用户的 macOS 与 Apple 账户设置决定。若不希望系统界面展示任何状态，请在“系统设置 → 通知”中关闭本应用通知。

## 保留与删除

控制命令超过 5 分钟即被拒绝和删除；应用启动时和运行期间会清理超过 1 小时的匹配确认文件、超过 7 天的连续失败隔离文件。应用未运行时无法执行定时清理。

应用内“清除全部本机守护数据”会删除守护列表、运行记录、额度缓存、控制命令、确认与隔离文件，不会删除 Codex 中的原任务。为避免破坏正在提交的 turn，自动续跑进行中会拒绝清除。

若要连同应用注册信息一起手动彻底删除：

1. 先从菜单栏退出 Codex 自动续跑守护；
2. 删除 `~/Library/Application Support/codex-resume-manager/`；
3. 如不再使用，再从 Codex 插件管理中卸载 `codex-auto-resume`。

删除应用本体不会自动删除上述应用数据目录。删除数据后，未完成的自动续跑不会继续，下一次启动会生成空白状态。

## 诊断与问题反馈

报告问题时不要附上真实的 `state.json`、命令、ACK、dead-letter、Codex 认证文件、数据库或会话记录。请先用虚构的 thread/turn ID、项目路径和任务内容制作最小复现；安全问题按 [SECURITY.md](./SECURITY.md) 私下报告。

## 授权边界

本项目与 OpenAI 不存在隶属、授权或背书关系。仓库默认按私有项目管理，未授予开源许可证并保留全部权利。
