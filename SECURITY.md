# 安全说明

## 支持范围

当前只维护仓库最新版本。历史构建、被修改的 Hook、非官方转载包和自行更改安全边界的派生版本不在支持范围内。

应用目前生成 unsigned macOS `.app`，未签名、未公证。源码可以在受控环境中审查和构建，但不要把未知来源的同名应用当作本项目构建。

## 私下报告安全问题

请优先使用仓库的 GitHub Security Advisory 私下报告。若该功能不可用，请通过维护者提供的私下联系方式报告，不要创建包含利用细节或私人数据的公开 Issue。

报告建议包含：

- 受影响版本和 macOS/Codex 版本；
- 风险影响与可复现的最小步骤；
- 使用虚构 thread/turn ID、目录和任务内容的样例；
- 已尝试的缓解方法。

请不要提交或粘贴：

- `auth.json`、访问令牌、API Key 或环境变量；
- Codex SQLite、rollout/JSONL、完整对话或隐藏推理；
- 本机 `state.json`、commands、acknowledgements 或 dead-letter 原文件；
- 包含真实用户名、项目路径、任务标题或工作内容的截图。

## 安全模型

本项目的信任边界是当前登录的 macOS 用户：

- Hook 只把声明式的开启、关闭和状态命令交给后台，不执行来自任务文本的任意命令；
- Hook 必须由用户在 Codex `/hooks` 中明确信任；
- 后台只把顶层结构化额度错误作为自动恢复依据；
- 自动恢复前会检查失败 turn、线程变化、Codex 版本和项目目录；
- 无法证明操作是否已经提交时会安全停止，避免重复执行；
- 无人值守时不会自动批准不可见的人工审批；
- 状态与控制文件限制为当前用户访问。

同一 macOS 用户下运行的恶意程序通常拥有读取用户文件或操纵本地进程的能力，不属于本项目能够建立的强隔离边界。请只安装可信插件和应用，不要在共享或已失陷账户中依赖自动续跑。

## 开发验证

提交变更前至少运行：

```bash
npm ci
npm run check
```

发布前还应检查：

- 仓库与构建包不含密钥、认证文件、本机状态、个人路径或内部项目名称；
- `release/`、`dist/`、`node_modules/` 和运行数据没有进入 Git；
- Hook manifest 与脚本来源一致，并在 Codex 中显示预期的信任状态；
- 应用包来自干净工作区，版本号与插件 manifest 一致。

## 授权

本仓库默认按私有项目管理，当前未授予开源许可证并保留全部权利。安全报告或补丁提交不构成对源代码的再分发授权。
